// Set this to the deployed rover-image-proxy URL.  Use '/nasa-image' if the
// Worker is routed on the same domain as this app. Never place secrets here.
window.MARS_ROVER_IMAGE_PROXY = { endpoint: '' };
