name = "mars-rover-image-proxy"
main = "worker.js"
compatibility_date = "2026-09-12"

# Set this after deployment. Comma-separate exact app origins, for example
# https://mars-rover-360.example.org
[vars]
ALLOWED_ORIGINS = ""
